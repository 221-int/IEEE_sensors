# 눈 깜빡임 감지 — 벡터 인코더 + 학습 판정 모델
# 초안. 인코더/판정 모델 구현체가 정해지면 버전 고정 필요.

numpy>=1.24.0
opencv-python>=4.8.0

# 눈 크롭 (얼굴/랜드마크 검출)
mediapipe>=0.10.0

# 학습 프레임워크 (택1 — 확정 후 다른 쪽 제거)
torch>=2.2.0
# tensorflow>=2.15.0

# ONNX 변환 + Pi5 런타임 추론
onnx>=1.15.0
onnxruntime>=1.17.0

# 벤치마크/실험
psutil>=5.9.0
matplotlib>=3.7.0

# Raspberry Pi 5 (64-bit Raspberry Pi OS / Bookworm):
#   sudo apt-get install -y python3-opencv libatlas-base-dev
#   pip install onnxruntime opencv-python numpy
