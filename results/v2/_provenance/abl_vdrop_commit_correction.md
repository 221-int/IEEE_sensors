# `abl_vdrop.json` 의 `env.git_commit` 은 틀렸다 (2026-08-06)

## 무엇이 틀렸나

| | 값 |
|---|---|
| JSON 이 기록한 커밋 | **`a9a1913`** (08-06 12:01) |
| **실제로 실행된 코드** | **`9bc811e`** (08-06 10:35) |
| 런 시작 / 종료 | 10:35:47 / 12:43:33 (127.8분) |

`env_fingerprint()` 는 **JSON 을 쓰는 시점**(12:43)에 호출된다. 그 사이에 커밋이
5개 쌓여서, 기록된 커밋이 실행 코드보다 **6개 앞선다.**

## 근거 (JSON 자체에서 확인 가능)

실행 중이던 프로세스는 10:35 에 메모리로 읽은 구 코드를 계속 썼다:

- `config` 에 **`front` 키가 없다** — `--front` 는 `f362cbd` (11:50) 에 추가됐다
- 런 지표가 `accuracy·precision·pr_auc·roc_auc·thr` 뿐이다 —
  `recall`·`f1` 은 `9da2e63` (11:53) 에 추가됐다

즉 이 런은 `9bc811e` 시점의 `train_encoder.py` 로 돌았다.

## 결과 수치에 영향이 있나 — **없다**

런 도중 `train_encoder.py` 에 들어간 변경은 두 가지뿐이고 둘 다 `ours` 경로를
바꾸지 않는다.

1. `--front` 분기 추가 — 기본값 `ours` 는 기존과 **비트 동일**함을 회귀 검증했다
   (sym16 1에폭: loss 0.2264 / val AP 0.9764 / ours 0.9741, 리팩터 전후 동일)
2. `recall`·`f1`·혼동행렬 **추가 저장** — 계산에 관여하지 않고 필드만 늘린다

따라서 `abl_vdrop.json` 의 PR-AUC 는 유효하다. **틀린 것은 지문이지 숫자가 아니다.**
다만 이 런에는 recall·f1 이 없다 — 필요하면 사이드카에서 후처리하거나 재실행한다.

## 왜 또 났나

`_provenance/README.md` (2026-08-03) 가 같은 실패를 기록하고
*"다음 런부터는 실행 전에 커밋해서 이 파일이 필요 없게 만들 것"* 이라고 적어 뒀다.
이번엔 **실행 전에 커밋은 했다**(`9bc811e`). 그런데 **런이 도는 동안 같은 파일을
계속 고쳤다.** 커밋 시점만 지키고 "런 중에는 실행 중인 파일을 건드리지 않는다"를
안 지켰다.

> **규칙 보강**: 런이 도는 동안 그 런이 임포트한 모듈
> (`train_encoder.py`, `model/encoder.py`, `common/*`, `dataset/crop.py`)을 수정하지 않는다.
> 급하면 **런이 끝난 뒤에** 고치거나, 브랜치를 파서 거기서 고친다.
> 무관한 파일(`deploy/*`, 문서)만 수정한다.

## 다른 런은 괜찮나

| 런 | 상태 |
|---|---|
| `abl_sym16` · `abl_vpres_ref` · `abl_vfull` · `dim_D8/32/64` | ✅ 문제 없음. `34bf530` 에서 시작해 끝날 때까지 `src/` 를 안 고쳤다 |
| `abl_vdrop` | 🔴 이 문서의 대상 |
| `imgcnn_*_pilot` | 12:43 이후 시작. 시작 시점 코드가 `a9a1913` 이고 **그 뒤로 `src/v2/train_encoder.py`·`model/encoder.py` 를 고치지 않는다** |
