# 실행 중 런의 코드 출처 보존 (2026-08-03 13:0x)

`train_encoder.RUNNING.cpython-312.pyc` 는 **현재 실행 중인 5fold×3seed 런이
실제로 돌리고 있는 바이트코드**다. 보존한 이유:

- `src/v2/train_encoder.py` 는 **git 미추적(untracked)** 이라 커밋으로 고정돼 있지 않다.
  따라서 결과 JSON 의 `env.git_commit` 은 그 런의 코드를 지목하지 못한다.
- 2026-08-03 에 그 파일을 패치했다(점수 사이드카 저장 / 런별 증분 기록 /
  earhead 가중치 저장). 실행 중 프로세스는 메모리의 구 코드를 계속 쓰므로
  **디스크의 소스와 실행 중인 코드가 다르다.**

| | 값 |
|---|---|
| 실행 코드 소스 mtime | 2026-08-02 21:53:00 |
| 실행 코드 소스 크기 | 17,731 bytes |
| 패치 후 소스 크기 | 22,543 bytes |
| Python | 3.12 (학습 환경) |

이 런의 결과를 인용할 때는 위 pyc 를 근거로 삼는다.
다음 런부터는 **실행 전에 커밋**해서 이 파일이 필요 없게 만들 것.
