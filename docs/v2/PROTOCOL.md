# v2 PROTOCOL — 규칙이 사는 유일한 곳

> mEBAL2 58명 단독 라운드. **규칙이 두 곳에 적히면 언젠가 갈라집니다.** 임계값·시드·
> 분할·표집·보고 규칙은 이 문서에만 적고, 코드는 `src/v2/common/` 하나만 씁니다.
> 실험 스크립트가 규칙을 직접 구현하는 것을 금지합니다.
>
> 실행: 리포지토리 루트에서 `python -m src.v2.<모듈>`
> 산출물 경로: `src/v2/` `results/v2/` `docs/v2/` `data/processed/v2/` `models/v2/`
> v1 (`src/`, `results/`, `models/`, `docs/` 최상위) 은 읽지도 쓰지도 않습니다.

---

## 0. 현재 상태

| 항목 | 상태 |
|---|---|
| Phase −1 (코드 규율) | ✅ 구현 완료, 게이트 **PARTIAL** (G-1g 는 torch 환경에서 재실행 필요) |
| Phase 1 (CSV-only 58명) | ✅ 완료 — `results/v2/phase1_csv58.json` |
| EAR 베이스라인 + δ | ✅ **확정** — δ=0.02, 주 지표 PR-AUC, 베이스라인 drop_ratio (§9-1) |
| Phase 0 (사전 프로브) | 미착수 — 영상 크롭이 선행 |
| 설계안 선택 | **보류** — 억제 설계는 이번 라운드 범위 밖 (§11) |
| 결측 프레임 정책 | **미정** — §7 |
| 밝기 정규화 | **미정** — §8 |

---

## 1. 임계값 격자

**구현**: `src/v2/common/thresholds.py` — 여기 밖에서 `np.linspace` 로 격자를 만들면 규칙 위반입니다.

### 1-1. 격자는 근사하지 않는다

최적 임계값은 **항상 데이터 점 위에** 있습니다. 그래서 유일 점수값이
`max_exhaustive`(기본 50,000) 이하이면 **유일값 전체 + 중점**을 격자로 씁니다.
근사 모드(분위수)는 표본이 그보다 클 때만 쓰는 대비책입니다.

이 결정의 근거는 Phase −1 게이트 실측입니다 (`results/v2/gate_minus1.json`,
음성 10,000개 기준, FA≤1% 구간에서 구별 가능한 FA 값 100개):

| 격자 | 격자점 | 실제로 짚은 FA 값 | 해상도 활용률 |
|---|---:|---:|---:|
| **exhaustive (v2 채택)** | 40,001 | **100** | **100.0%** |
| 분위수 logit 257, 클래스별 | 773 | 60 | 60.0% |
| 분위수 logit 257, 섞인 분포 | 259 | 23 | 23.0% |
| 분위수 선형 97 | 99 | 10 | 10.0% |

> **기록**: 이 표의 3번째 줄이 제가 Phase −1에서 처음 채택했던 설계입니다. 양 끝이
> 촘촘한 logit 분위수면 저-FA 구간을 짚을 줄 알았는데, 섞인 분포의 양 끝은
> '아주 확실한 음성'과 '아주 확실한 양성'이고 낮은 false-alarm 을 결정하는
> **음성 분포의 위쪽 꼬리는 한가운데**에 있었습니다. 게이트가 숫자로 잡았습니다.

### 1-2. 부호 규약

`canonical(score, higher_fires)` 를 입구에서 한 번 통과시켜 **항상 "높으면 발화"** 로
통일합니다. 이후 모든 코드는 `pred = score >= thr` 하나만 씁니다.
EAR 처럼 낮을수록 발화하는 점수는 `higher_fires=False` 로 넘깁니다.

### 1-3. 선택과 보고의 분리

- `select_threshold(val_score, val_y, ...)` — **val 배열만** 받습니다
- `evaluate_at(test_score, test_y, thr)` — **얼린 스칼라만** 받습니다

test 점수로 격자를 만들 경로가 API 에 존재하지 않습니다. 이게 의도입니다.

### 1-4. ours 와 baseline 은 같은 격자 함수를 통과한다

`GridAudit` 이 한 런에서 서로 다른 격자 **설정**이 쓰이면 종료 시 예외를 냅니다.
설정에는 `class_aware`(라벨을 줬는가) 도 포함됩니다 — 한쪽만 라벨을 주면
조용히 불공정해지므로 그것도 혼용으로 잡습니다.

---

## 2. 시드·결정성

**구현**: `src/v2/common/repro.py`

```python
repro.ensure_hashseed()   # 엔트리 스크립트 최상단. 필요하면 프로세스를 재실행합니다
repro.seal(seed)          # random / numpy / torch / cuda + cudnn.deterministic
```

`seal()` 이 잡는 것: `random`, `numpy`, `torch`, `torch.cuda`, `PYTHONHASHSEED`,
`cudnn.deterministic=True`, `cudnn.benchmark=False`,
`torch.use_deterministic_algorithms(True)`, `CUBLAS_WORKSPACE_CONFIG=:4096:8`.

`use_deterministic_algorithms` 는 **warn_only=False 가 기본**입니다. 결정적 구현이
없는 연산에서 조용히 넘어가지 않고 예외로 죽습니다.

DataLoader 는 반드시 `repro.dataloader_kwargs(seed)` 를 씁니다. generator 를 안 주면
shuffle 순서가 전역 RNG 상태(앞에서 난수를 몇 번 뽑았는지)에 의존합니다.

### 보고 규칙

- 모든 결과 JSON 에 `repro.env_fingerprint()` 를 통째로 넣습니다 (git commit·dirty 포함)
- **`deterministic: false` 인 런의 ± 는 보고에 쓰지 않습니다**
- 최소 **3시드**. 단일 시드로 baseline 과 비교하지 않습니다
- 조건 간 차이가 시드 std 보다 작으면 결론은 **"미측정"** 이라고 씁니다. "기각"이 아닙니다

---

## 3. 분할 규칙 (축마다 다릅니다)

| 축 | 분할 | 이유 |
|---|---|---|
| **유용성** (blink/unblink 분류) | **피험자 분리** 5-fold 회전 | 사람을 처음 보는 조건에서 재야 배포 성능 |
| **프라이버시** (58-way 재식별) | **피험자 공유 + 시간 분리** (세션 앞 70% / 뒤 30%) | 폐쇄집합 분류기를 학습하려면 58명 전원이 train 에 있어야 함 |
| 프라이버시 대조 | 무작위 분할 | 시간 분할과 **나란히** 보고. 두 숫자의 격차가 단서 |

**5-fold 회전**: `fold_rotation(i)` → test = fold i, val = fold i+1, train = 나머지 3개.
val 과 test 가 분리되므로 "val 에서 골라 얼려 test 에 적용"이 구조적으로 강제됩니다.

**무작위 분할 해석 주의**: 시간 분할과 격차가 작게 나와도 그것이 "신원이다"의
증거가 되지 않습니다. "이 분할 쌍으로는 신원과 세션을 못 가른다"는 뜻일 수 있습니다.
그래서 §9 의 (밝기, 선명도) 프로브가 필요합니다.

---

## 4. fold 고정

`src/v2/common/folds_5fold.json` 에 얼려서 커밋합니다. 생성은 `gate_minus1` 이 합니다.

이벤트 수가 사람마다 10 ~ 1,055 개로 **105배** 차이나므로
(`results/v2/facts_mebal2_events.json`), 무작위로 자르면 fold 간 이벤트 수가
중앙 1.81배 벌어집니다. 이벤트 수 LPT 층화로 **1.008배**까지 맞췄습니다.

| fold | 0 | 1 | 2 | 3 | 4 |
|---|---:|---:|---:|---:|---:|
| blink 이벤트 | 2,867 | 2,858 | 2,877 | 2,880 | 2,882 |
| 인원 | 11 | 11 | 12 | 12 | 12 |

시드를 쓰지 않는 결정적 배정입니다. fold 는 데이터의 성질이지 실험 조건이 아니므로,
시드를 두면 "어떤 시드의 fold 였나"가 또 하나의 자유도가 됩니다.

> **사용자를 제외할 때 fold 를 다시 짜지 마십시오. 마스크로 빼십시오.**
> 다시 짜면 이전 결과와의 비교가 끊깁니다.

---

## 5. chance 보고 규칙 (필수)

**정확도를 보고할 때 균등 chance 만 쓰는 것을 금지합니다.**

mEBAL2 58명은 이벤트 수가 불균형이라, 아무것도 안 배운 분류기가 최다 사용자만
찍어서 **0.0734** 를 얻습니다. 1/58 = 0.0172 를 chance 로 쓰면 성능을 4.3배
과대평가하게 됩니다 (`results/v2/gate_minus1.json` `splits.chance_unbalanced`).

`splits.chance_report(y)` 가 항상 함께 냅니다.

| 필드 | 의미 |
|---|---|
| `uniform_chance` | 1/클래스수 — **균형 표집일 때만** 유효 |
| `majority_baseline` | 최다 클래스 비율 — **항상** 유효한 하한 |
| `balanced` | 클래스별 개수가 모두 같은가 |

**보고 문장에는 두 값을 다 씁니다.** 예: `재식별 0.31 (uniform 0.017 / majority 0.017, 균형 표집)`

---

## 6. 균형 표집 규칙 (재식별 프로브)

`splits.balanced_subject_sample(subject, n_per_subject, seed, min_required)`

- **기본 조건**: 전원 유지, 피험자당 `min(N_u, 50)` 이벤트 표집
- **민감도 조건**: 50개 미만 5명 제외 (`min_required=50`) → 53-way
- 두 숫자가 크게 다르면 그 자체를 보고 대상으로 삼습니다

---

## 7. 결측 프레임 정책 — **미정**

이벤트 19프레임 중 몇 개까지 결측을 허용하고 언제 이벤트를 버릴지.

**지금 정하지 않습니다.** 알려진 결측률 8.8% 는 **User 1 한 명** 값이고 58명 분산은
미측정입니다. 고정 임계값을 미리 박으면, 결측률이 높은 특정 사용자가 통째로
날아가는 것을 모른 채 진행하게 됩니다.

→ **Phase 1 에서 58명 결측 분포를 잰 뒤 이 절에 확정 임계값을 적습니다.**
확정 전에는 Phase 3(대량 전처리)로 넘어가지 않습니다.

---

## 8. 밝기 정규화 — **미정**

`to_input_tensor` 가 `/255` 만 할지, 사용자별 밝기 정규화를 넣을지.

Phase 0 (d) 프로브와 Phase 1 F비로 결정합니다 (§11 게이트).

참고로 배포자 제공 크롭 58장(피험자당 1장) 실측은 밝기 **54.0 ± 20.3 (22.3~105.9)**
입니다 (`results/v2/facts_mebal2_events.json` `provided_crops_58`). 다만
크기가 제각각인 제공 크롭이고 피험자 내 분산이 미측정이라, 이 값으로 결정하지 않습니다.

---

## 9. 필수 대조군

새 실험을 만들 때 **대조군을 먼저 정합니다.** 단일 지표는 거의 항상 오해를 만듭니다.

### 9-1. EAR 판정 기준 = 비열등(non-inferiority). **우월이 아니다**

우리 표현이 EAR 을 **이길 필요는 없습니다.** 기여 문장이 *"동일 유용성 조건에서 신원
노출량을 정량화한다"* 이므로, 유용성 동등은 주장이 아니라 **실험의 전제 조건**입니다.
다만 유용성이 의미 있게 낮으면 애초에 대체재가 아니므로 하한은 남습니다.

    판정:  (ours − EAR) 차이의 fold 부트스트랩 95% CI 하한 > −δ   ->  비열등
           CI 가 −δ 를 걸침                                        ->  판정 유보
           CI 하한 > 0                                             ->  우월 (주장하지 않아도 됨)

**"차이가 유의하지 않다"는 동등의 근거가 아닙니다.** 표본이 적거나 분산이 크면 진짜로
나빠도 차이가 안 잡히고, 그러면 측정을 대충 할수록 동등해 보이는 역설이 생깁니다.

### 🔒 δ = 0.02 · 주 지표 = PR-AUC — **2026-08-01 확정, 재협상 없음**

| 항목 | 값 |
|---|---|
| **주 지표** | **PR-AUC** (임계값 선택과 무관) |
| **마진 δ** | **0.02** |
| 판정 | (ours − EAR) PR-AUC 차이의 **피험자 클러스터 부트스트랩 95% CI 하한 > −0.02** |
| 함께 보고 | ROC-AUC · accuracy · precision (판정에는 쓰지 않음) |
| 확정 시점 | **우리 인코더 결과가 0건인 시점.** 어떤 편향도 들어갈 수 없었다 |

**근거** — 베이스라인의 fold 간 변동이 세 지표에서 ROC-AUC 0.0166 / PR-AUC 0.0203 /
accuracy 0.0190 으로 모두 0.02 근처였다(`results/v2/ear_baseline_folds.json`).
δ 가 이보다 작으면 측정 잡음에 묻혀 어떤 방법도 판정을 못 받고, 훨씬 크면
"사실상 구분 불가 수준"이라는 반박을 받는다. **"EAR 이 fold 를 바꿔도 이만큼은
흔들린다"** 가 δ 의 의미다.

**부트스트랩은 반드시 짝지어(paired) 계산한다.** 같은 재표집 표본에서 ours 와 EAR 을
둘 다 재고 그 **차이**를 부트스트랩한다. 두 방법의 CI 를 따로 구해 비교하면
피험자 효과가 상쇄되지 않아 불필요하게 넓어진다. `stats.subject_bootstrap()` 에
차이를 돌려주는 metric 을 넘기면 된다.

### EAR 베이스라인 실측 (58명, `results/v2/ear_baseline_folds.json`)

| 특징 | fold accuracy | fold ROC-AUC | 피험자 부트스트랩 AUC (95% CI) |
|---|---|---|---|
| **drop_ratio ← 채택** | 0.8486 ± 0.0190 | **0.9091 ± 0.0166** | 0.9093 [0.8927, 0.9255] |
| min_ear | 0.6731 ± 0.0315 | 0.7361 ± 0.0406 | 0.7251 [0.6814, 0.7808] |

- **베이스라인은 drop_ratio(창 안 상대 하강)를 쓴다.** min_ear(절대 임계값)보다 AUC 가
  0.17 높다. **약한 변형을 골라 놓고 이기면 그것은 방법의 성능이 아니다.**
- val 낙관 편향 **+0.0043** (임계값 1개짜리라 작다. 학습된 인코더에서는 커질 것)
- 사용자별로 따로 재면 중앙 0.930 이지만 **섞으면 0.909** — 배포 조건(처음 보는 사람)의 값이 후자다
- ⚠️ 이벤트 **28,518 / 28,728** 만 사용됐다. 창 양끝이 둘 다 결측이라 하강률을 정의할 수
  없는 210개(0.73%)가 빠졌고, 그 결과 blink 14,245 / unblink 14,273 으로 **1:1 균형이
  미세하게 깨졌다**. 머리 움직임이 큰 구간이 선택적으로 빠졌을 수 있으므로 §10 에 민감도 항목으로 둔다.

| # | 대조군 | 무엇을 방어하나 |
|---|---|---|
| 1 | **EAR 스칼라 × 19프레임 → 동일 판정 헤드** | **비열등 기준(§9-1)을 충족하지 못하면** 기여는 "표현"이 아니라 "학습된 판정 규칙" |
| 2 | 크롭 → 소형 CNN 직접 | 표현 단계의 기여 |
| 3 | 동일 차원 random projection | 학습의 기여 |
| 4 | 평균 벡터 (정보 0) | 하한 |
| 5 | 랜덤 초기화 인코더 | 목적함수 가설의 생사 |
| 6 | 원본 픽셀 / PCA-32 | 재식별의 천장 |
| 7 | **(밝기, 선명도) 2차원** | **세션 광학 서명 vs 신원** |
| 8 | **t_rel 단독 분류기** | 세션 시각 교락 (§10 참조) |

EAR 은 **mEBAL2 제공 랜드마크**로 계산합니다. 같은 검출기·같은 얼굴 선택 규칙을
써야 "다른 검출기라서 졌다"는 반론이 봉쇄됩니다.

**안경 편향 경고**: 우리 방법은 크롭을 직접 보고 EAR 은 랜드마크에 의존하므로,
안경에서 EAR 이 더 크게 망가질 수 있습니다. 즉 **편향 방향이 우리에게 유리합니다.**
→ 전체 / 착용 / 미착용 **3개 숫자를 항상 함께** 냅니다.
**전체에서 이기고 미착용군에서 지면 그건 베이스라인이 불리했던 것**이고, 기여 주장에서 뺍니다.

---

## 10. 필수 민감도 분석

실측에서 나온 구조적 위험들입니다 (`results/v2/facts_mebal2_events.json`).

| # | 조건 | 왜 |
|---|---|---|
| S1 | `Blink=0` 1,323개 포함 | 기본은 제외. 포함 조건을 병기 |
| S2 | 클래스 시간 위치 편차 \|Δ\|>0.2 인 3명 제외 | User 46 은 편차 **0.495** — 양성이 세션 전반, 음성이 후반에 몰림. 눈꺼풀이 아니라 **세션 시각**으로 분리될 수 있음 |
| S3 | 클래스 겹침 unblink 1,135개 제외 | 음성 창의 7.9% 가 실제 깜빡임 프레임을 1~3장 포함 |
| S4 | 50이벤트 미만 5명 제외 | 시간 블록 분할 시 test 이벤트가 3개뿐인 사용자 존재 |
| S5 | 분당 깜빡임 <2 인 4명 제외 | 라벨이 부분적임이 확실 (생리학적으로 불가능) |
| S6 | 창 양끝 결측으로 빠진 210개 이벤트 | EAR 베이스라인에서 0.73% 가 정의 불가로 제외됐다. 머리 움직임이 큰 구간이 선택적으로 빠졌을 수 있다. 우리 방법은 이 이벤트도 처리 가능하므로 **양쪽을 같은 집합으로 맞춰 비교**해야 한다 |

---

## 11. 게이트

통과 못 하면 **멈춥니다.** 각 게이트는 숫자입니다.

### Phase −1 (완료, G-1g 제외)

| 게이트 | 기준 | 결과 |
|---|---|---|
| G-1a fold 균형 | max/min ≤ 1.05 | **1.0084** PASS |
| G-1b fold 결정성 | 두 번 계산해 동일 | PASS |
| G-1c 격자 해상도 | FA 해상도 활용률 ≥ 99.9% | **100.0%** PASS |
| G-1d 격자 혼용 차단 | 설정·라벨 비대칭 모두 예외 | PASS |
| G-1e chance 불균형 감지 | majority > 4× uniform 을 잡아냄 | PASS |
| G-1f 균형 표집 회복 | 표집 후 balanced=True | PASS |
| G-1 부호 규약 | higher/lower 결과 동일 | PASS |
| G-1h 투영 제거 | 피험자 간 산포 < 5% 로 감소 | 4.463 → 0.0000 PASS |
| **G-1g 학습 결정성** | 같은 시드 2회 **비트 동일** | **SKIP — torch 환경에서 재실행 필요** |

### Phase 0 → 설계 분기 규칙 — ⚠️ **보류**

아래 세 안(A 동역학 / B 이산 코드 / C 억제)은 **전부 신원 억제 설계**라, 억제를 이번
라운드에서 다루지 않기로 한 이상 지금 고를 일이 없습니다. 억제를 이번 범위에
포함할지 박사님 확인이 오면 되살립니다.

**이번 라운드의 인코더는 억제 기전 없는 기준선 하나**입니다 — 19프레임 시퀀스 입력,
깜빡임 지도학습, 연속 실수 병목. 그 위에서 EAR 비열등 판정과 재식별 정량화를 합니다.

| (보류) Phase 0/1 결과 | 채택 설계안 |
|---|---|
| (d) 밝기·선명도 2차원 프로브 > 0.30 | A 동역학 — 세션 서명이 주범이면 입력에서 지우는 게 먼저 |
| (a) 랜덤 초기화 인코더 ≈ 균형 chance | C 억제 — 목적함수가 원인일 여지 있음 |
| 위 둘 다 아님 | B 이산 코드 (기본값) |

추가 정지 조건: **(b) 원본 픽셀 프로브 < 0.20 이면 멈춥니다** — 픽셀조차 신원을
못 담는다면 표집이나 라벨링이 잘못된 것입니다.

### 이후 게이트

| Phase | 게이트 |
|---|---|
| 1 | 밝기 F비 > 5 → 정규화 필수 분기 / INTER_CUBIC 경로 사용자 > 5명 → 크롭 배율 재검토 / **랜드마크 눈꼬리 오차 중앙값 > 3 px → 제공 랜드마크로 학습 불가** |
| 2 | 크롭 이상 사용자 ≤ 3명 / 창 중앙 EAR 최저 사용자 ≥ 45/58 |
| 3 | Blink=1 = 14,364, Unblink = 14,364, 1:1 = 58/58 재현 / 폐기 이벤트 < 10% |
| 4 | **대조군 1(EAR×19)에 대해 비열등 판정(§9-1) 통과** / 시드 std > 조건 차이면 결론은 "미측정" |
| 5 | 재식별 4칸(선형·MLP × 시간블록·무작위) + 집계 공격 N∈{1,10,100} **보고 완료**. 특정 임계값 미달을 실패로 두지 않는다 — 이번 라운드에서 재식별은 **보조 검증이자 정량화 대상**이지 성립 조건이 아니다 |
| 6 | p99 e2e ≤ 33.3 ms @ 1280×720 |

**이번 라운드의 연구 성립 조건 = Phase 4 게이트(EAR 비열등) 하나입니다.**
여기서 통과하지 못하면 표현이 대체재가 아니므로, 재식별을 정밀하게 재도
"쓸모없는 표현이 얼마나 새는가"밖에 되지 않습니다.

### 후속 연구용 게이트 — **이번 라운드에서 쓰지 않음 (박사님 확인 대기)**

신원 억제 기법과 유용성–프라이버시 파레토 곡선은 교수님 방향의 후속입니다
(`docs/PROJECT.md` §2·§3, 2026-08-01). 아래는 그 단계에 가서야 유효합니다.

| 게이트 (후속) | 기준 |
|---|---|
| 파레토 성립 | 유용성이 대조군1 대비 비열등을 유지하면서 재식별이 균형 chance의 5배 미만(< 0.086)인 점이 1개 이상 |
| 억제 기전 검증 | 사후 투영 제거 전/후 × 선형/MLP 4칸. **선형만 내려가고 MLP 는 그대로면 프라이버시 기전이 아니라 프로브 회피** |

---

## 12. 측정하지 않는 것

데이터가 허용하지 않습니다. 계획에 넣지 마십시오.

- 연속 영상 event recall / frame false-alarm — 라벨 커버리지 문제. 미라벨을 음성으로
  놓고 재면 모델이 아니라 **라벨링 밀도**를 재게 됩니다
- PERCLOS, 불완전 깜빡임 비율 — 이벤트가 전부 고정 19프레임 창이라 **지속시간 라벨이 없습니다**

## 13. 논문 서술 규칙

- 인원은 반드시 **"a 58-subject subset of mEBAL2"**. mEBAL2 전체가 180명임을 밝힙니다.
  **"180명 대규모 검증"은 사실이 아닙니다**
- 이전 라운드(8명 공개셋)의 수치를 v2 결과와 같은 표에 올리지 않습니다
- 지연시간은 v2 에서 새로 측정한 것만 인용합니다

---

## 부록 — 반복하지 말 것

1. 실험 스크립트마다 격자를 따로 만들지 말 것 (`src/v2/common/thresholds.py` 만)
2. 격자를 근사하지 말 것 (표본이 5만 이하면 유일값 전체)
3. 단일 시드로 baseline 과 비교하지 말 것 (최소 3시드)
4. val 에서 고르고 val 에서 보고하지 말 것
5. 봉인 안 된 런의 ± 를 인용하지 말 것
6. chance 를 1/58 로만 쓰지 말 것 (majority 병기)
7. 새 실험은 대조군을 먼저 정할 것
8. 결과가 유리하면 베이스라인이 불리해진 것은 아닌지 먼저 의심할 것
9. **n=1 값(User 1)을 58명 값처럼 쓰지 말 것** — 검출 결측 8.8%, box/landmark 불일치
   6.0%, 크롭 밝기 34.8~45.4, span 125~132 px, 안경 EAR 하강폭은 전부 User 1 한 명입니다
10. 문서가 아니라 파일을 열어 확인할 것
