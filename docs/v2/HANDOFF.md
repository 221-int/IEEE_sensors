> 🔵 **2026-08-08 — 현재 상태는 [`docs/STATUS_2026-08-08.md`](../STATUS_2026-08-08.md) 가 기준이다.**
> Phase 3(성능 축)은 전부 종결됐다. 이 문서의 이전 서술 중 그와 어긋나는 부분은 낡은 것이다.
> 새 세션은 [`docs/NEXT_SESSION_PROMPT.md`](../NEXT_SESSION_PROMPT.md) 를 쓴다.

# 다음 세션 인계 — 새 채팅에 붙여넣을 프롬프트

> 🔵 **2026-08-05 범위 정정.** 아래 [최종 목표] 블록은 **2026-08-04 판이며 폐기되었다.**
> 2026-08-05 미팅에서 논문 범위를 **눈 깜빡임 검출 + 에지 성능 검증**으로 축소했고,
> 신원 억제·복원 장벽은 특허·후속 연구로 분리했다.
> 현재값: **[`docs/PROJECT_DIRECTION.md`](../PROJECT_DIRECTION.md)** ·
> [`docs/TASKS.md`](../TASKS.md) · [`docs/CHANGELOG.md`](../CHANGELOG.md)

## 🔵 최신 인계 상태 — 2026-08-08

아래가 이 문서의 기존 `[끝난 것]`·`[아직 안 된 것]` 목록보다 우선한다.

- **확정 구조:** `vpres / D=16` 유지.
- **vdrop:** vpres와 성능 차이가 없고 더 싼 대조군으로 기록한다. 실제 Pi 측정에서
  G-E1(e2e p99 ≤ 33.3 ms)을 실패할 때만 배포 후보로 전환한다.
- **T3-1:** image_cnn 탐색 비교와 `image_cnn_head`·`image_cnn_max` 확정 런 완료.
- **T3-6:** ours 확정 런 재현, ear_head 가중치, Recall/F1, 혼동행렬, 이벤트 가중
  서브그룹 판정 완료.
- **배포 준비:** `src/v2/deploy/`의 네 모드(`ours`, `ear`, `image_cnn_max`,
  `image_cnn_head`)와 ONNX 동치 게이트 완료.
- **Pi 5 측정(T5-8):** 완료. `ours`, `ear`, `image_cnn_max`, `image_cnn_head`의 480p/720p
  실측과 G-E1 판정을 끝냈으며, 상세 수치는 논문 문서의 Table II-b에 기록되어 있다.
- **현재 상태:** Pi 측정 결과에 따라 `vpres`를 유지한다. `vdrop`은 G-E1 실패 시의 대체 후보다.

근거: `results/v2/train_encoder_final.json`, `results/v2/train_image_cnn_head_final.json`,
`results/v2/train_image_cnn_max_final.json`, `results/v2/posthoc_subgroups_final.json`,
`results/v2/export_onnx.json`, `results/v2/check_equivalence.json`.
>
> 아래 프롬프트를 쓰려면 **[최종 목표]와 [먼저 읽을 것] 블록을 아래 정정판으로 교체**한다.
> 나머지([끝난 것] · [아직 안 된 것] · [사고가 났던 것] · [출력 규칙])는 그대로 유효하다.
>
> ```
> [최종 목표 — 2026-08-05 정정]
> 원본 눈 이미지를 판정 단계에서 쓰지 않고, encoder 가 만든 embedding vector 만으로
> eye blink detection 을 수행하는 경량 모델을 만들고, 그것이 Raspberry Pi 5 같은
> edge device 에서 실시간으로 동작함을 보인다. 이것이 IEEE Sensors Letters 논문의 전부다.
> 주 비교 대상은 EAR 기반 방법 + 원본 이미지 기반 분류 모델(image_cnn)이다.
> ⚠️ 신원 제거·복원 방지·디코더 공격 방어는 논문 범위 밖이다(특허·후속 연구).
>    문서 어딘가에 "최종 목표는 신원 재식별 불가능"이 남아 있으면 낡은 것이다.
>
> [먼저 읽을 것 — 이 순서로]
> 1. docs/PROJECT_DIRECTION.md   ★ 연구 범위의 현재값
> 2. docs/TASKS.md               ★ 지금 무엇을 할지
> 3. docs/PROGRESS.md            무엇이 검증됐고 무엇이 안 됐는지
> 4. docs/v2/PROTOCOL.md         ★ 측정 규칙(§1~§13)의 유일한 기준. §0 상단 정정 헤더부터
>    ※ §14·§15 는 범위 밖이다. 읽되 이번 라운드 게이트로 적용하지 마라
> 5. 필요한 결과만 골라 읽어라. PROTOCOL §0 에 파일 경로가 전부 적혀 있다
> ```

---

```
저장소 C:\Users\sch\PycharmProjects\IEEE_sensors 에서 v2 라운드를 이어서 한다.

[최종 목표 — 이것부터 확인해라]   ⚠️ 2026-08-05 에 폐기됨. 위 정정판을 쓸 것
인코더 단에서 **신원 재식별을 불가능하게 만드는 것(프라이버시 확보)**이 최종 목표다.
프라이버시의 주 근거는 **복원 장벽(§14)** 이고, 재식별(§11 Phase 5)은 보조 지표다.
EAR 비열등은 주장이 아니라 **전제 조건**이다.
⚠️ 문서 어딘가에 "신원 억제는 범위 밖 / 박사님 확인 대기"가 남아 있으면 **낡은 것이다**
(2026-08-04 폐기). 발견하면 고쳐라.

[먼저 읽을 것 — 이 순서로]   ⚠️ 2026-08-05 에 폐기됨. 위 정정판을 쓸 것
1. docs/v2/PROTOCOL.md  ★ 규칙과 현재 상태의 유일한 기준. §0 → §14 → §15 순서로 읽어라
2. 필요한 결과만 골라 읽어라. §0 에 파일 경로가 전부 적혀 있다

[읽지 말 것]
- docs/PROJECT.md 의 §4·§5·§6·§7, docs/archive/**, results/*.json (v2/ 제외)
  전부 v1(Eyeblink8 8명) 기록이다. v2 의 근거가 되지 않는다.
- docs/v2/PROMPT_v2.md 는 "소비 완료" 표시가 붙어 있다. 본문 숫자를 인용하지 마라.
- docs/PI5_BENCHMARK.md 의 지연 수치는 v1 이다. v2 에서 다시 재기 전까지 인용 금지.

[끝난 것 — 다시 하지 마라]
- 데이터: mEBAL1 38명(2020-02) + mEBAL2 신규 20명(2022). 좌석 오염으로 U18 제외
  → 57명 / 유효 이벤트 27,758
- 확정(재협상 금지): 크롭 배율 2.2(64×160) / 결측 ≤5 / 프레임 표준화 /
  δ=0.02, 주 지표 PR-AUC / fold = 이벤트수+배치 이중 층화 57명 / 인코더 **구조** vpres
- ⚠️ **해제된 것**: 인코더 차원 D 와 목적함수는 🔓 (2026-08-04). v2 실측 근거가 없었고,
  최종 목표가 신원 억제이므로 §15 파레토에서 함께 정한다. **지금 확정하지 마라.**
- Phase −1 게이트 9/9 PASS. **G-1i(실제 인코더 학습 결정성) 통과 → ± 인용 가능**
- Phase 4 유용성: ours PR-AUC 0.9886 ± 0.0038 vs ear_head 0.9724 ± 0.0096,
  차이 +0.0151 [+0.0106, +0.0203] → 판정 superior
- 대조군 8 (t_rel 세션 시각 교락): 교락으로 우위를 설명할 수 없음
- U1 원인: 고유한 전이 실패 (train−test 격차 0.1798, 57명 최대. 중앙 0.0063)
- Phase 5 재식별: 57-way 0.6345 ± 0.0288 (chance 0.0175). 랜덤 인코더 0.6261 과 동급.
  픽셀 천장 0.9080 / 광학 바닥 0.0825 / EAR 0.0548. 집계 N=100 → 1.0000
  ※ 이 값들은 **억제 없는 기준선**이며 Phase 7 의 비교 기준점이다. 최종값이 아니다.
- 관련 연구 조사: docs/v2/RELATED_WORK.md (선형 부분공간 제거는 프로브 회피 위험,
  깜빡임 동역학 단독으로도 93.94% 식별됨 — "동역학만 쓰면 안전"은 성립 안 함)
- 랩미팅 슬라이드(docs/랩미팅_2026-08-05_슬라이드.md) v2 로 전면 재작성 완료

[아직 안 된 것 — 우선순위 순]   🔵 2026-08-05 재정렬. 상세는 docs/TASKS.md
- **대조군 2 = 원본 이미지 기반 분류 모델(image_cnn)** — 🔴 **최우선.** 논문의 주 비교다.
  없으면 "표현의 기여"와 "학습된 판정 규칙의 기여"를 분리할 수 없다 (T3-1)
- **embedding vector 차원 D 확정** — 파레토가 아니라 **깜빡임 성능 + edge 비용**으로 (T3-5)
- **경량화 지표** — 파라미터 수 · 모델 크기 · MMAC · v2 ONNX export (T5-1~T5-4)
- **Pi5 재측정** — v2 encoder 로. p99 e2e ≤ 33.3 ms. ours/ear/image_cnn 3자 (T5-6~T5-8)
- **관련 연구 재조사** — 에지/경량 깜빡임 검출 · encoder 기반 · 일반적 실패 조건 (T6-1~T6-3)
- **강건성** — 광학 층화(측정값 이미 있음) + 합성 저조도 (T4-1, T4-4)
- 대조군 3·4 (random projection / 평균 벡터) (T3-2, T3-3)
- ▶ **범위 밖으로 이동**: Phase 5-bis 복원 장벽(§14), Phase 7 억제·파레토(§15),
  게이트 G-R·G-P → docs/PATENT_AND_FUTURE_WORK.md
- 서브그룹 풀링 δ 판정 (ear_head 상대) — 이번 런이 원시 점수를 저장 안 함.
  사이드카 패치는 적용돼 있으니 재실행(~4.5h)하면 얻어진다
- 얼굴 선택 규칙의 시간적 일관성(근본 원인). 사후 플래그로만 막고 있다
- git 커밋 — src/v2/ 대부분이 미추적이라 결과 JSON 의 git_commit 이 실행 코드를
  지목하지 못한다. 새 실행 전에 커밋해라

[이 프로젝트에서 실제로 사고가 났던 것 — 반복하지 마라]
1. 결과가 유리하면 베이스라인부터 의심해라. 우위가 +0.1058 → +0.0323 → +0.0151 로
   두 번 줄었다(대조군을 동일 판정 헤드로, 고정 에폭을 동일 중단 규칙으로 바꾸면서)
2. 표본이 대표하는지 먼저 확인해라 (User 1 하나를 58명 대표로 쓴 것 등 3회)
3. 선형 프로브만 보지 마라 (광학 선형 0.0767 / MLP 0.2018)
4. 격자·시드·분할·프로브는 src/v2/common/ 하나만 쓴다
5. val 에서 고르고 val 에서 보고하지 마라
6. chance 를 1/57 로만 쓰지 마라. 균등과 다수 클래스를 병기한다
7. 문서가 아니라 파일을 열어 확인해라. **그리고 새 스크립트를 짜기 전에 같은 일을
   하는 스크립트가 이미 있는지 먼저 확인해라** (u1_audit.py 를 모르고 중복 구현했다)
8. 실행은 리포지토리 루트에서 python -m src.v2.<모듈>
9. **게이트가 검사 대상을 쓰지 않는지 확인해라.** 2026-08-03 에 게이트가 얼린 fold 와
   CUDA 실행 기록을 각각 한 번씩 지웠다
10. 서브그룹은 이벤트 가중(또는 풀링)을 주 숫자로 내라. 피험자 평균은 작은 피험자를
    과대 가중한다 (U1 이 안경군 이벤트의 1.1% 인데 피험자 평균에서 5.9%)
11. "학습에 넣으면 잘 맞힌다"를 대조군 없이 해석하지 마라. 전원이 ~1.0 이다

[출력 규칙]
- 칭찬하지 마라. 동의 여부보다 무엇이 틀릴 수 있는지를 말해라
- 추측 금지. 확인된 것만 쓰고 불확실하면 "미확인"이라고 명시해라
- 모든 수치에 출처 파일 경로를 병기해라
- 코드를 고치면 왜 고쳤는지, 무엇이 틀렸었는지 먼저 말해라
- 마지막 줄에: 가장 확신 없는 지점 1개
```

---

## 붙여넣기 전 확인

- [ ] `git add -A && git commit` 으로 현재 상태를 커밋했는가
- [ ] 장시간 실행을 걸어둔 것이 없는가 (있으면 몇 %까지 갔는지 프롬프트에 적어라)
- [ ] PROTOCOL §0 의 "진행 중 / 남은 것" 표가 실제 파일 상태와 맞는가
