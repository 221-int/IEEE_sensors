# Claude Code 다음 지시 — 붙여넣기용

> 작성 2026-08-06. `image_cnn_head` 6런이 끝나는 시점 기준.

---

```
image_cnn_head 가 끝나면 아래 순서로 진행해라. 순서를 바꾸지 마라.

────────────────────────────────────────
[0] 사람 판단 항목 회신 — 확정 arch·D (§8 #5·#6)

**vpres / D=16 을 유지한다.** 근거:

- 절제 결과가 §3-quater 사전 등록의 **갈래 A** 다. vpres−vdrop +0.0010
  [−0.0032, +0.0069] 로 세로 해상도 논거는 지지되지 않았고, D8~64 도 구분 불가다
- 다운스트림(확정 런 점수 사이드카·U1 분석·서브그룹)이 전부 vpres 위에 있다.
  바꾸면 연쇄 재실행이고 8월 말 일정에서 그럴 여유가 없다
- 데스크탑 실측에서 encode 가 e2e 의 4% 다. MMAC 을 72% 줄여도 e2e 는 약 3% 움직인다.
  **비용 이득이 실익으로 이어지지 않는다**
- 전환 조건은 하나다: **Pi 실측에서 G-E1(e2e p99 ≤ 33.3 ms)을 못 맞출 때.**
  그때 vdrop 을 꺼낸다. 기준은 MMAC 이 아니라 Pi 실측이다

단, **Table II 에 vdrop 행을 함께 싣는다.** 심사자가 "더 싼 모델이 동등한데 왜 비싼 걸
배포했나"를 반드시 묻는다. 먼저 표로 답한다 — 인코더는 정확도 손실 없이 3.6배 싸게
만들 수 있고, 그럼에도 e2e 는 거의 변하지 않는다. 인코더가 병목이 아니라는 증거가 하나 는다.

────────────────────────────────────────
[1] T3-1 마무리

세 방법(ours / image_cnn_max / image_cnn_head)을 한 표로 묶고 T3-1 을 닫아라.

논문 서술에 반드시 반영할 것:
- **"임베딩이 이미지보다 정확하다"는 쓸 수 없다.** ours 는 image_cnn_head 보다 근소하게 낮다
- 쓸 수 있는 것은 **"동등한 정확도를 2.6배 적은 연산과 5.7배 적은 파라미터로 얻는다"**
  (δ=0.02 기준 non_inferior, CI 하한 −0.0083 > −0.02)
- **"문헌 방법 대비 +0.0735"에는 각주가 필수다** — 원문은 프레임별 open/closed 라벨로
  학습했고 우리는 창당 라벨 하나(max-pooling MIL)라 문헌 방법을 과소평가했을 수 있다
- 두 추정량이 갈리는 지점(풀링 −0.0048 / 런평균 −0.0016 ± 0.0026, 시드 std 0.0031)은
  **"미측정"으로 적는다.** 어느 쪽으로도 단정하지 마라

────────────────────────────────────────
[2] 미뤄둔 코드 수정 — **런이 멈춘 지금 한다**

(a) env_fingerprint 구조 수정
    `env_fingerprint()` 를 main() 시작 시점에 **1회 호출해 저장**하고, write_partial 과
    최종 JSON 이 그 값을 재사용하게 해라. 지금은 JSON 쓰는 시점에 호출돼서
    abl_vdrop 때 같은 사고가 재발할 수 있다. write_partial 은 런마다 호출되므로
    **부분 JSON 도 지문이 흔들린다.** 규율(파일 안 건드리기)에만 의존하지 마라.

(b) "인과적 TCN" 서술 정정
    `src/v2/model/encoder.py` docstring 이 "인과적 TCN(dilation 1·2·4)" 이라고 적고 있는데
    **실측으로 틀렸다** (t=18 을 바꾸면 t=17 출력이 4.72 바뀐다. 대칭 패딩이라 창 안에서
    미래를 본다). 정확한 표현은 **"19프레임 링버퍼 위의 양방향 TCN. 미래 프레임을
    기다리지 않으므로 시스템 추가 지연이 없다"** 다. "인과적 스트리밍"도
    **"미래를 기다리지 않는(non-lookahead) 스트리밍"** 으로 고쳐라.

────────────────────────────────────────
[3] 🔴 image_cnn 을 Pi 에서 잴 수 있게 만들어라 — **Table II 의 핵심 열**

지금 `run_video.py` 는 `--mode {ours, ear}` 뿐이고 `models/v2/onnx/` 에도
ours 것(encoder.onnx + head.onnx)만 있다. **image_cnn 3자 비교를 못 잰다.**

(a) `export_onnx.py` 에 image_cnn 경로 추가 → `models/v2/onnx/image_cnn/` 로 export
(b) `run_video.py` 에 `--mode image_cnn` 추가
(c) `check_equivalence.py` 에 image_cnn 경로도 통과시켜라 (G-Q1~G-Q4 동일 기준)

⚠️ **지연 측정에는 학습된 가중치가 필요 없다.** 탐색 런에 --save-models 를 안 붙였으니
image_cnn 체크포인트가 없을 텐데, **구조만 같으면 지연은 같다.** 무작위 초기화로 export 하되
결과 JSON 에 `weights: "random (latency only)"` 를 **반드시 기록해라.** 정확도 수치와
섞이면 안 된다.

⚠️ `image_cnn_max` 와 `image_cnn_head` 는 백본이 같고 시간 처리만 다르다.
지연에서 갈리는 부분이 어디인지(head 유무) 단계별로 분해해서 내라.

────────────────────────────────────────
[4] Pi 측정용 클립 준비 (PC 에서)

`run_video.py` 는 영상을 **반복 재생하지 않는다** (`if not ok: break`).
그런데 클립이 둘 다 5분 측정에 부적합하다:

- 640×480: `data/_legacy_public/eyeblink8/eyeblink8/9/27122013_152435_cam.avi`
  = 5,183프레임 ≈ 2분 53초 → **모자란다**
- 1280×720: `data/raw/mEBAL2/User 1/RealSense/Color_Webcam/color.mp4`
  = **4.2 GB** → 그대로 못 옮긴다

`clips/` 를 만들고 아래를 준비해라. **재인코딩 금지(`-c copy`)** — 화질이 바뀌면
검출 성능이 달라진다.

  720p: ffmpeg -ss 0 -t 320 -i "<color.mp4>" -c copy -avoid_negative_ts make_zero \
                clips/mebal2_u1_720p_320s.mp4
  480p: eyeblink8 #9 를 2회 이어붙여 5분 이상으로 (concat demuxer, -c copy)

만든 뒤 **해상도·fps·프레임수·길이를 찍어서 확인**하고 JSON 이나 로그에 남겨라.

────────────────────────────────────────
[5] T3-6 확정 재실행 — **[2](a) 수정 이후에**

- 확정 arch·D = **vpres / D=16**
- `--save-models` 로 돌린다 → `models/v2/fold*_seed*/` 에 **earhead_front.pt,
  earhead_head.pt** 가 생겨야 한다 (현재 encoder.pt, head.pt 뿐이다)
- recall/f1·혼동행렬이 이번 런부터 저장되는 것은 확인됐다
- ⚠️ **돌리기 전에 `models/v2` 를 백업해라.** MODELS 가 하드코딩이라 덮어쓴다
- 끝나면 posthoc_subgroups 를 재실행해 **이벤트 가중 풀링 + ear_head 상대 δ 판정**을 내라

────────────────────────────────────────
[출력 규칙]
- 칭찬하지 마라. 무엇이 틀릴 수 있는지를 말해라
- 모든 수치에 출처 파일 경로를 병기해라
- 탐색(fold 0·1) 숫자를 확정 값으로 인용하지 마라
- 마지막 줄에: 가장 확신 없는 지점 1개
```
